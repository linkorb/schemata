{% extends "base.html.twig" %}

{% block content %}
    <h2>Page index</h2>
    <input id="searchInput" autofocus class="form-control" style="margin-top: 10px; margin-bottom: 10px;" placeholder="Search... "/>
    <table>
        <tbody id="table-body">
        {% for name in names %}
            <tr>
                <td><a href="{{ name }}">{{ name }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
